### 2024-05-24 - [2.3 Implémenter le système de combat - amassage de ressource] | Implement AoE Combat | AI updated `Player.cs` to apply immediate AoE damage via a `HashSet` to deduplicate overlapping areas and bodies. Updated all resources and enemies to implement the Core `IDamageable` interface, properly hooking into `StatManager` for health and `ScoreManager` for enemy deaths. | Used `HashSet` to prevent double-damage bugs caused by Godot's overlapping body/area detection logic returning multiple colliders for the same entity. Relocated `IDamageable` to Core to follow N-Tier strict interface rules.### 2024-05-24 - [2.3 Implémenter le système de combat - amassage de ressource] | Fix NPC Wall Phasing & Resource Persistence | Analyzed collision masks to determine missing wall Layer 1, and implemented UI hydration via global `InventoryManager` inside `InventoryUi._Ready()` | Decision Reasoning: Direct modification of `.tscn` is avoided per instructions, so a manual Godot editor guide was provided. For UI, fetching state directly on node creation guarantees accuracy upon scene loading.
### 2026-04-29 - [User Story 2.3] Implémenter le système de combat - amassage de ressource | Request: Create unified AoE combat logic for dealing damage to enemies and resources, and handle drop/score rewards. Refactor hit flashes. | AI Contribution: Developed generic `PlayHitFlash` extension, refactored NPCs (Sheep, Soldier) and Resources (Gold, Rock, Trees) to use it. Removed Soldier gold drops while retaining score. Added documentation. | Decision Reasoning: Used static extension method for `CanvasItem` to apply hit flash uniformly across codebase without duplicating code.
