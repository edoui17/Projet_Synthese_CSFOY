### 2024-05-24 - [2.3 Implémenter le système de combat - amassage de ressource] | Implement AoE Combat | AI updated `Player.cs` to apply immediate AoE damage via a `HashSet` to deduplicate overlapping areas and bodies. Updated all resources and enemies to implement the Core `IDamageable` interface, properly hooking into `StatManager` for health and `ScoreManager` for enemy deaths. | Used `HashSet` to prevent double-damage bugs caused by Godot's overlapping body/area detection logic returning multiple colliders for the same entity. Relocated `IDamageable` to Core to follow N-Tier strict interface rules.