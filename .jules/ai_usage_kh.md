# AI Usage Journal

### Task Date - [User Story 5.2 : Implémenter le système de point]

| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| **User Story 5.2: Implémenter le système de point.**<br>- Implement score accumulation, positive value validation, UI update signal, high score persistence, and leaderboard preparation.<br>- **Architectural Decisions**: Implement "Live State vs Configuration" (Core vs Godot Resource) and "Dependency Inversion" for the save system (`ISaveService`). Implement a Score Bridge Pattern via `ScoreManager`. | **1. Core Domain Models**: Implemented `SessionState` (`Src/Core/Domain/`) to strictly hold mutable runtime data (Score, MapCount, CharacterId).<br>**2. Dependency Inversion**: Created `ISaveService` in Core and implemented `GodotSaveService` in Godot using `FileAccess` (`user://` prefix), injected at runtime.<br>**3. Core Managers**: Built `ScoreTracker` (`Src/Core/Managers/Stats/`) handling score accumulation, positive validation logic, and persisting high scores via the injected save service. Exposed `WeakEvent` for changes.<br>**4. Godot Components**: Added `SessionResource` (`Src/IslandSurvivor/Resources/Stats/`) as an immutable starting template. Created `ScoreManager` (`Src/IslandSurvivor/Nodes/StatsManager/`) acting as a bridge, transforming `WeakEvent` to Godot `[Signal]`.<br>**5. UI Testing**: Provided a simple `ScoreUI` script linking to the signal for console output testing.<br>**6. Verification**: Wrote comprehensive unit tests (`ScoreTrackerTests`) using xUnit and Moq to validate core logic and high score preservation without Godot dependencies. | **Live State Isolation**: By keeping `SessionResource` strictly as a starting configuration, we avoid mutating Godot Resources at runtime, preserving them as templates. The Core's `SessionState` becomes the sole source of truth during gameplay, greatly simplifying JSON serialization.<br>**Engine Agnosticism**: The `ScoreTracker` in the Core tier knows nothing about Godot's file system or rendering. By injecting `GodotSaveService`, the logic remains fully portable (e.g., to a Blazor Web context later).<br>**Memory Safety**: The Bridge pattern using `WeakEvent` prevents memory leaks caused by strong delegate references between the Core tier and Godot's C# Nodes, while seamlessly integrating with Godot's signal system for UI updates. |

### 2024-05-18 - [US 3.1: Procedural Map Generation]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| Implement procedural island generation using N-Tier architecture. | Designed `IMapData`, `IMapGenerator`, `ISpawnLocator` interfaces in Core. Implemented `GodotIslandGenerator` utilizing `FastNoiseLite` and a BFS flood fill for land connection. Created a test scene demonstrating the Godot-Core translation. | Godot specific functionality (like Noise generation) was implemented strictly on the Godot client side via Dependency Inversion. Standard primitive strings were chosen for the Godot-Core bridge to keep the Core independent of Godot Tilemaps. |

### 2024-05-18 - [US 3.1: Procedural Map Generation - Elevation]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| Implement Plateaus, Cliffs, and Stairs to Map Generation. Ensure connectivity. | Updated `TileTypeConstants`. Overhauled `GodotIslandGenerator` to apply a second threshold for plateaus, trace borders to make cliffs, and inject stairs. Updated BFS to consider plateaus/stairs as walkable. Updated `ProceduralMapTest` to draw these onto `ElevationTileMap`. | A multi-pass approach (noise -> borders -> connectivity) was best to guarantee constraints. By mapping these directly to `ElevationTileMap` using ID 1, we effectively layered visuals exactly as requested in `map_1.tscn` without making the generator aware of Godot layers. |

### 2024-05-18 - [US 3.1: Procedural Map Generation - Splash Transitions]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| Add a splash/foam animation transition between water and land. | Added neighbor-checking logic to `ProceduralMapTest.cs`. When placing a water tile, it scans the surrounding 8 tiles. If any are land (Ground, Plateau, Cliff, Stairs), it draws a foam tile on the `FoamWaterTileMap` layer. | Placed this logic entirely in the Godot script. The Core doesn't need to know about "Foam" because it's a purely visual representation of a coastline. This respects the N-Tier architecture while achieving the desired visual effect. |

### 2024-05-18 - [US 3.1: Génération et Rendu (map_1.tscn)]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| Lier l'algorithme de génération à la scène Godot `map_1.tscn` via un `MapRenderer`. | 1. Création de `MapRenderer.cs` attaché à `Map1`.<br>2. Création de `GameManager.cs` en Autoload pour injecter et fournir le `MapManager`.<br>3. Utilisation de `SetCellsTerrainConnect` en batching pour les layers `GroundTileMap` et `ElevationTileMap`.<br>4. Logique d'adjacence manuelle pour l'écume (`FoamWaterTileMap`) posée sous la terre.<br>5. Positionnement dynamique du `SpawnMarker`. | **Séparation des responsabilités :** Le `MapManager` (Core) génère les données (`IMapData`), et seul le `MapRenderer` (Godot) traduit ces `strings` en appels visuels (`SetCellsTerrainConnect`, `SetCell`).<br>**Optimisation :** L'utilisation d'une liste `Vector2I` passée à `TerrainConnect` permet à Godot de calculer automatiquement les transitions organiques (Match Corners and Sides) efficacement.<br>**Architecture :** L'utilisation de `GameManager` comme Singleton/Autoload respecte le besoin de rendre la donnée de map accessible globalement sans coupler le rendu à la génération. |
### 2026-04-09 - [US 3.2 : Implémenter la navigation entre les îles]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| **US 3.2 Navigation**<br>- Add navigation system between islands.<br>- Track current island and session data.<br>- Support resource cost deduction and persistence across island transitions. | **1. Core Models:** Added `IslandDestination` record for static and dynamic destination configs. Extended `SessionState` and `ScoreTracker` to track `CurrentIslandId`.<br>**2. Core Logic:** Implemented `NavigationService` logic to deduct resources and emit a weak event `OnNavigationRequested`. Generated random islands with varied stats, ensuring at least one free option.<br>**3. Godot Bridge:** Extended `SignalManager` to relay navigation signals.<br>**4. UI/Interaction:** Developed `NavigationMenu.tscn` accessible from the base map UI. Converted `InteractionScript` to use `IInteractable` interface. Created `PortalInteraction.cs` that reacts to destination selection and actually triggers the map jump.<br>**5. Godot Integration:** Created `NavigationManager` (autoload) to handle Godot scene switching and to call `GodotSaveService` to serialize inventory and session state before transitioning. | **Safe Navigation Flow:** Instead of switching scenes immediately when purchased, the menu passes the destination to a visual Portal object. The player interacts with the portal to trigger the actual Godot scene transition, keeping gameplay grounded.<br>**Persistent State:** Intercepted the scene transition in `NavigationManager` to inject `ISaveService` calls, ensuring the inventory array and the updated session are safely committed to disk before Godot purges the scene tree.<br>**Decoupled Signals:** Kept the Godot client loosely coupled by using `WeakEvent` to `Signal` translation for cross-island navigation. |
