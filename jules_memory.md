When refactoring state machines in Godot C# where states are dynamically resolved from `StateMachine.GetState()`, ensure to check types and set properties (e.g. `NextStateAfterWindup`) on states properly. Also, replace string variables and `.tscn` references reliably. When splitting combat states (e.g., `AttackState` into `MeleeAttackState` and `RangedAttackState`), ensure the associated `AttackController` handles ranged unit attacks gracefully, and consider extracting projectile logic into a separate `Shooter` node to decouple it from `AttackController`.
