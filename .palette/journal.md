## 2026-05-25 - Error Dialog Keyboard Focus & Juice
**Learning:** For static Control nodes loaded during `_Ready` like `m_retryBtn` and `m_offlineBtn` inside a CanvasLayer popup, focus navigation needs to be explicitly wrapped for keyboard users so they aren't trapped if focus falls out of bounds (e.g. `FocusNeighborRight/Left`). Additionally, when using `.CreateTween()` to scale buttons for Game Juice, `PivotOffset = btn.Size / 2f` must be set immediately before the tween executes to ensure it scales cleanly from the center rather than the top-left origin.
**Action:** Always setup cyclic `FocusNeighbor` arrays/paths for distinct popup choice buttons, and update `PivotOffset` immediately on `FocusEntered` prior to animating `.scale`.

## 2026-05-25 - Error Dialog Keyboard Focus & Juice
**Learning:** For static Control nodes loaded during `_Ready` like `m_retryBtn` and `m_offlineBtn` inside a CanvasLayer popup, focus navigation needs to be explicitly wrapped for keyboard users so they aren't trapped if focus falls out of bounds (e.g. `FocusNeighborRight/Left`). Additionally, when using `.CreateTween()` to scale buttons for Game Juice, `PivotOffset = btn.Size / 2f` must be set immediately before the tween executes to ensure it scales cleanly from the center rather than the top-left origin. Always track and kill existing tweens to prevent visual jitter.
**Action:** Always setup cyclic `FocusNeighbor` arrays/paths for distinct popup choice buttons, update `PivotOffset` immediately on `FocusEntered` prior to animating `.scale`, and manage active tweens by killing them before creating a new one.
## 2026-05-21 - [Resource Inventory Updates Feel Static]
**Learning:** In Godot Control nodes, when numeric values in the inventory (like resource amounts) change rapidly, the instantaneous update feels disconnected. Creating dynamic `Tween` animations on `Label` properties (like scale and modulate) adds necessary juice and immediately draws the player's attention to state changes. It is crucial to kill existing tweens before creating new ones (`_currentTween?.Kill()`) to avoid visual glitching or memory leaks if the value changes rapidly before the previous animation completes.
**Action:** When updating numeric or state-based HUD elements, always implement a minor fallback checking mechanism for previous state (`_lastAmount`) and use a controlled, parallel `CreateTween()` sequence for scale and color shifting to highlight the change cleanly.

## 2026-05-28 - [Tweening UI Scale and Layout Jitter]
**Learning:** When adding hover or focus scaling tweens to Godot UI controls, setting `PivotOffset = btn.Size / 2f` immediately before `CreateTween()` ensures the scale happens from the exact center, preventing layout jitter or misaligned expansion. Additionally, including utility icon buttons (like 'Show Password') in explicitly managed focus loops (`FocusNeighborLeft/Right/Top/Bottom`) is essential to prevent keyboard users from skipping them or becoming trapped.
**Action:** Always recalculate and assign `PivotOffset` immediately before initiating a scale tween on UI buttons, and explicitly include all interactive elements (including tiny icon-only toggles) in the focus navigation web, ensuring they have descriptive `TooltipText`.
