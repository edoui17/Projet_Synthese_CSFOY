### 2024-03-XX - [US-14] Ajout de feedback visuel et sonore | Implemented visual and audio feedback via AudioManager and Tween-based Resource Drops | AI Contribution: Developed `ResourceDrop.cs` to tween materials to the player upon harvest. Integrated `AudioManager` calls in Enemy, Sheep, Player, and Resource scripts. Added Godot Editor setup guide in `WikiCode`. | Decision Reasoning: Used `GD.Load<AudioStream>` with `AudioManager.Instance` rather than local `AudioStreamPlayer2D` children to prevent audio cutoff when the parent node is destroyed (e.g., via `QueueFree` upon death). Created a discrete `ResourceDrop` Node2D to decouple visual collection timing from instant underlying data updates, invoking `EmitMaterialDestroyed` only upon Tween completion.