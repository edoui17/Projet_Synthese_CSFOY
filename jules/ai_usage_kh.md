[Output truncated for brevity]

| Fix `CS1503`, `CS7036`, and `CS1501` compilation errors caused by new `IEventBus` constructor requirements and `TryNavigate` signature change. | Implemented `EventBus` instantiation in `ServiceRegistry`, injected it into Core managers, and set up `_Process` for deferred event processing. Refactored `NavigationMenu.cs` to fetch `NavigationService` from `ServiceRegistry.Instance` and updated `TryNavigate` calls. Fixed all corresponding unit tests. | Centralizing infrastructure instantiation in `ServiceRegistry` respects N-Tier architecture and DI principles. Overriding `_Process` guarantees deferred events execute correctly in the Godot lifecycle. Removing `InventoryManager` from `TryNavigate` cleanly decouples navigation logic from inventory validation, delegating communication to the EventBus. |
### 2026-04-22 - [Navigation System Fix]
**Request**: Fix the navigation system so that islands cannot be purchased without sufficient resources, ensuring N-Tier separation.
**AI Contribution**: Implemented synchronous resource checking in `NavigationService` and `NavigationMenu` by injecting `ShopManager` and `InventoryManager` into `NavigationService`. Updated the Godot UI to disable buttons dynamically, and updated unit tests.
**Decision Reasoning**: The `NavigationMenu` now correctly queries `NavigationService.CanAffordIsland()` to keep UI decoupled from shop logic. The asynchronous transaction processing is maintained in `InventoryManager` via EventBus, but now the initial request is securely blocked if resources are inadequate.
### 2026-04-22 - [Navigation Menu UI Revert]
**Request**: Remove the text modifications and disabling of UI buttons in the Navigation Menu when the player cannot afford an island. Prefer using GD.Print logs and keeping UI active.
**AI Contribution**: Reverted visual alterations inside `NavigationMenu.cs` so that the button strictly displays the destination data.
**Decision Reasoning**: Retained the core functionality of blocking the backend purchase via the synchronous check in `NavigationService`, but restored the UI text so it doesn't clutter the user interface.

### 2026-04-23 - [Système] Bridge Architecture Refactor
**Request**: Implement the pub-sub architecture strictly. Refactor the SignalManager to act as a bridge between Godot Signals and the EventBus, ensuring Core managers no longer use WeakEvents directly.
**AI Contribution**: Analyzed the project architecture. Deleted obsolete `ISignalManager` and `SignalManagerCore`. Created required `IEvent`s. Migrated `ScoreTracker`, `StatTracker`, and `NavigationService` to rely strictly on the `IEventBus`. Refactored Godot `SignalManager` to translate native Godot signals into `IEvent`s and vice-versa. Updated Global Godot Nodes (`ScoreManager`, `StatManager`) to directly subscribe to the `EventBus` for updates. Updated Unit tests to match the new dependencies.
**Decision Reasoning**: In N-Tier architecture, the Core layer must remain completely agnostic of engine-specific concepts (like Godot Signals). Providing an event bus ensures loose coupling, and the Godot-side SignalManager translates interactions cleanly without bleeding engine paradigms into Core business logic.

### 2026-04-26 - [US 8.1 Data Persistence via API & DB]
- **Request**: Implement the data synchronization logic to communicate with the existing database schema and API, redirecting the local save logic to act as a fallback cache.
- **AI Contribution**:
  1. Removed default WeatherForecast endpoint from `API/Program.cs` and enabled standard controllers mapping.
  2. Implemented `IApiService` and `ApiService` for HTTP `LoginAsync`, `GetProfileAsync`, and `SyncAsync`.
  3. Integrated `ApiService` into Godot's Autoload `ServiceRegistry` to fetch and publish `ProfileLoadedEvent` upon startup.
  4. Updated `NavigationManager` to construct a `SyncRequest` from the core managers and post it via `ApiService.SyncAsync` upon every scene transition.
- **Decision Reasoning**: Preserving `ISaveService` inside `ApiService` satisfies the offline fallback requirement. Executing sync calls during scene transitions leverages the existing, safe synchronization barrier (`NavigationManager` handling deferred loads). By generating new `IEvent` payloads (`SyncStartedEvent`, etc.), the architecture strictly honors the Pub-Sub boundaries instead of introducing coupling.

### 2026-04-27 - [US 9.1 Application des modifications de statistiques sur les entités (Itération 1: Vitesse)]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| Implémenter l'augmentation de vitesse lors de l'achat d'amélioration, en respectant l'architecture N-Tier. Créer les tests unitaires et intégrer avec le Godot Client. | 1. **Refactoring Core (TDD)** : Création de l'interface `IStat`, renommage de `Stat` en `PoolStat` (pour HP) et création de `AttributeStat` (sans Max ni Current variable) pour les statistiques statiques (comme la vitesse). Mise à jour du `StatTracker` et des `UnitTests`.<br>2. **Intégration Client Godot** : Modification de `MovementController.cs` et de la fonction Fallback de `Player.cs` pour interpréter la valeur de l'attribut de vitesse de manière mathématique en pourcentage de bonus de déplacement (1pt = +5% vitesse) appliqués sur une vitesse de base fixe de 300f. | La scission de la classe `Stat` en une architecture par interface garantit le respect de SRP (Séparation des Responsabilités) entre les différents types de Stats. Le système du côté de Godot est conçu pour lire uniquement la valeur brute de `StatTracker` et l'appliquer en modificateur local pour éviter les interférences avec le calcul en pourcentage lors d'une sauvegarde ou d'un affichage UI pur de la Stat. |

### 2026-04-27 - [US 9.1 Application des modifications de statistiques sur les entités (Itération 2: Attaque)]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| Implémenter le modificateur de dégâts lors d'une attaque, basé sur la statistique `Attack` en respectant l'architecture N-Tier. | 1. **Update Core Tests** : Ajout du test `AttackStat_IncrementsCorrectly_WithAttributeStatLogic` dans `StatTrackerTests.cs` pour confirmer que la statistique `Attack` bénéficie bien de la logique statique de `AttributeStat` sans valeur max/courante. <br>2. **Intégration Client Godot** : Refactorisation de la logique de collision des armes dans `Player.cs`. Ajout de `BaseDamage` en variable `[Export]`. Remplacement de `IAttackable.OnAttacked` par l'interface standardisée `IDamageable.TakeDamage`. Implémentation du calcul: Dégâts Finaux = Dégâts de Base * (1f + (Attaque * 0.05f)) avec un arrondi mathématique (`Mathf.RoundToInt`) avant l'application sur la cible. | Le remplacement de `IAttackable` par `IDamageable` unifie la gestion des dégâts sous une seule interface capable de transporter l'information de la valeur du dégât calculée dynamiquement. Le maintien de `BaseDamage` dans le Client Godot via `[Export]` préserve l'agnosticisme du Core concernant l'équilibrage de l'entité du jeu, tandis que la récupération de la valeur statique d'Attaque se fait proprement via le `StatsManager`. |

### 2026-04-27 - [US 9.1 Application des modifications de statistiques sur les entités (Itération 3: Santé)]
| **Request** | **AI Contribution** | **Decision Reasoning** |
| :--- | :--- | :--- |
| Implémenter l'augmentation de Santé lors de l'achat d'amélioration de manière à ce que 1 point acheté octroie +10 HP (Max et Current) en respectant l'architecture N-Tier. Mettre à jour l'UI de la barre de vie en conséquence. Ajouter les tests unitaires. | 1. **Modification du Godot Client** : Mise à jour de `StatManager.cs` pour vérifier le type de stat et octroyer +10 de façon permanente si `StatType.Health` est concerné, +1 sinon. Remplacement des calculs internes de `HealthBarStatic.cs` par une souscription à l'événement global de mise à jour des stats. Ajout de `IDamageable` à `Player.cs` pour soustraire la vie via `Stats.ModifyCurrentValue`. <br>2. **Refactoring EventBus** : Migration de `StatTracker.cs` (Core) pour utiliser le pattern N-Tier via `IEventBus` au lieu de `WeakEvent`. Mise en place du relais dans `SignalManager.cs`. <br>3. **Unit Tests** : Ajout d'un test s'assurant que l'ajout du bonus permanent affecte correctement le ratio max/current de `PoolStat` même si l'entité est blessée. | Le relais par `SignalManager` (Bridge Pattern) maintient le couplage faible entre Core (C# pur) et Godot (UI et `TextureProgressBar`). La soustraction de point de vie via l'interface standardisée `IDamageable` permet aux entités comme les `Rock` ou les `Sheep` d'appliquer les mêmes méthodes d'attaque sur le joueur. |

### 2026-04-28 - [User Story 9.1] Système d'Amélioration Permanente des Statistiques
- **Requête :** Implémenter l'effet de la statistique "Chance" (Luck) sur le système de butin et mettre à jour les tests unitaires.
- **Contribution de l'IA :** Création des tests pour la statistique Chance en tant que `AttributeStat` sans logique de valeur maximale. Modification de la logique de destruction des ressources et de la mort des ennemis (Gold, Rock, Trees, Soldier, Sheep) pour récupérer la valeur de la Chance via le `StatTracker` de `ServiceRegistry` et appliquer +5% de chance de drops supplémentaires par point, en ajoutant la quantité entière plus une chance probabiliste d'obtenir la portion fractionnaire.
- **Raisonnement :** L'approche utilise la séparation entre `AttributeStat` (qui n'a pas de limite maximale, comme la Chance) et `PoolStat` (qui a un maximum, comme la Santé). Cela permet un comportement correct dans `StatTracker`. Le système RNG ajoute dynamiquement des ressources bonus sans modifier les classes de domaine (comme `ResourceItem`) qui ne possèdent pas nativement de propriété de rareté.

### 2026-04-28 - [Tâche : Refactorisation majeure du système de statistiques (StatManager)]
- **Request**: Refactor StatManager to eliminate the global singleton coupling. Give each entity its own localized StatTracker and EventBus. Update stats to use modular inheritance (EntityStats, CombatEntityStats, PlayerStats). Make Godot's UI react to local signals. Drop resource calculations must fetch luck from the attacker.
- **AI Contribution**: Removed the global `StatTracker` instantiation from `ServiceRegistry`. Implemented isolated `EventBus` and `StatTracker` in `StatManager` node and created a `LocalStatChanged` Godot signal that binds to local entities UI. Created the `EntityStats` inheritance structure for specific properties. Refactored `TakeDamage` to pass the `attacker` object, which is then used in resource dropping logic to extract the attacker's Luck multiplier. Re-routed global UI (like the Player HUD) to dynamically connect to the Player's specific StatManager.
- **Decision Reasoning**: Centralizing the stats into individual entities strictly resolves the singleton data-bleeding issue. To comply with Godot conventions and maintain N-tier purity, we use a Godot native signal locally while retaining the pure C# EventBus within each `StatManager`. Connecting the global `InventoryManager` via the attacker's stats directly preserves the localized loot multiplier feature.
### 2026-04-30 - [Tâche : Refactorisation majeure du système de statistiques (StatManager)]
- **Request**: Refactor StatManager to eliminate the global singleton coupling. Give each entity its own localized StatTracker and EventBus. Update stats to use modular inheritance (EntityStats, CombatEntityStats, PlayerStats). Make Godot's UI react to local signals. Drop resource calculations must fetch luck from the attacker.
- **AI Contribution**: Removed the global `StatTracker` instantiation from `ServiceRegistry`. Implemented isolated `EventBus` and `StatTracker` in `StatManager` node and created a `LocalStatChanged` Godot signal that binds to local entities UI. Created the `EntityStats` inheritance structure for specific properties. Refactored `TakeDamage` to pass the `attacker` object, which is then used in resource dropping logic to extract the attacker's Luck multiplier. Re-routed global UI (like the Player HUD) to dynamically connect to the Player's specific StatManager.
- **Decision Reasoning**: Centralizing the stats into individual entities strictly resolves the singleton data-bleeding issue. To comply with Godot conventions and maintain N-tier purity, we use a Godot native signal locally while retaining the pure C# EventBus within each `StatManager`. Connecting the global `InventoryManager` via the attacker's stats directly preserves the localized loot multiplier feature.
